# 档案管理系统 - 环境变量配置示例
# 复制此文件为 .env 或 .env.local 并填入实际值
# 注意：.env 和 .env.local 文件不应提交到git

# ==================== 数据库配置 ====================
# MySQL数据库配置
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here

# ==================== Redis配置 ====================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# ==================== RabbitMQ配置 ====================
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# ==================== MinIO配置 ====================
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# ==================== JWT密钥配置 ====================
# 警告：生产环境必须使用强随机密钥
# 可以使用以下命令生成：openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_min_32_chars_recommended_64_chars

# ==================== 微信登录配置 ====================
WECHAT_APP_ID=wx1234567890abcdef
WECHAT_APP_SECRET=your_wechat_app_secret
WECHAT_REDIRECT_URI=http://your-domain.com/api/auth/wechat/callback

# ==================== Spring Profile ====================
# 环境配置：dev(开发), test(测试), prod(生产)
SPRING_PROFILES_ACTIVE=dev

# ==================== 生产环境额外配置 ====================
# 以下配置仅在生产环境需要

# 文件存储路径
FILE_STORAGE_PATH=/data/archive/files
FILE_TEMP_PATH=/data/archive/temp

# 备份路径
BACKUP_PATH=/data/archive/backup

# 数据库服务器（生产环境）
PROD_DB_HOST=prod-db-server
PROD_DB_PORT=3306

# Redis服务器（生产环境）
PROD_REDIS_HOST=prod-redis-server
PROD_REDIS_PORT=6379

# RabbitMQ服务器（生产环境）
PROD_RABBITMQ_HOST=prod-mq-server
PROD_RABBITMQ_PORT=5672

# ==================== 安全建议 ====================
# 1. 所有密码必须使用强密码（至少16位，包含大小写字母、数字、特殊字符）
# 2. JWT_SECRET 建议使用64位以上的随机字符串
# 3. 生产环境的密钥绝不能与开发环境相同
# 4. 定期更换密码和密钥
# 5. 使用密钥管理服务（如AWS Secrets Manager, Azure Key Vault）存储敏感信息

