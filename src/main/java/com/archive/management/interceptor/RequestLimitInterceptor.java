package com.archive.management.interceptor;

/**
 * 请求限流拦截器（RateLimitInterceptor的别名）
 * 为了兼容性保留此类
 */
public class RequestLimitInterceptor extends RateLimitInterceptor {
    
    public RequestLimitInterceptor() {
        super();
    }
}

